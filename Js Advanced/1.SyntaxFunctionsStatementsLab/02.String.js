function length(a,b,c){

    let totawl = a+b+c;
    let length = totawl.length;
    let avg = Math.floor(length/3);

    console.log(length);
    console.log(avg);
}

length('chocolate', 'ice cream', 'cake');